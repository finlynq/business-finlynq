// Vitest runs in Node. This preserves the production `server-only` marker in
// application modules while providing the same empty server condition Next
// supplies during its server compilation.
export {};
